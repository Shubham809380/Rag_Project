# Cross-origin deployment (frontend on Vercel, backend on Render):
VITE_API_URL=https://your-backend.onrender.com/api

# Same-origin deployment (frontend + backend on same domain):
# VITE_API_BASE_URL=/api
