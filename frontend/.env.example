# Same-origin deployment (frontend + backend on same domain, e.g., Vercel):
VITE_API_BASE_URL=/api

# Cross-origin deployment (frontend and backend on different domains):
# VITE_API_URL=https://your-backend-domain.com/api
